[package]
name = "main_binary"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1.44.2", features = ["full"] }
blockchain_simulator = {path = "../blockchain_simulator"}
blockchain_client_server = {path = "../blockchain_client_server"}
tracing = { version = "0.1.41", features = ["async-await"] }
tracing-subscriber = { version = "0.3.19", features = ["env-filter"] }
